using UnityEngine;

public class SetCursor : MonoBehaviour
{
    //[SerializeField]
    //private Texture2D cursor = null;

    //[SerializeField]
    //private Vector2 hotspot = new Vector2(12f, 9f);

    private void Awake()
    {
        //UnityEngine.Cursor.SetCursor(cursor, hotspot, CursorMode.Auto);
    }
}
